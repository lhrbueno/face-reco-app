const _CLIENT = 'pg';
const _HOST = '127.0.0.1';
const _USER = '';
const _PASSWORD = '';
const _DATABASE = ''

const DATABASE_CONNECTION = {
  client: _CLIENT,
  connection: {
    host: _HOST,
    user: _USER,
    password: _PASSWORD,
    database: _DATABASE
  }
}

module.exports = DATABASE_CONNECTION;